'use strict';
var even_group_calculate_average = function (collection) {
  // var arry = collection.filter(value => (collection.indexOf(value) + 1) % 2 === 0).filter(number => number % 2 === 0);
  // var res = [];
  // const sum = (a, b) => (a + b) / 2;
  // for (var i = 0; Math.pow(10,i) < collection.sort()[collection.length - 1]; i++) {
  //   res.push(arry.filter(number => number > Math.pow(10, i) && number < Math.pow(10, i + 1)).reduce(sum));
  // }
  // return res;
};
module.exports = even_group_calculate_average;
